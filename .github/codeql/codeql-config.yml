# Exclude NCB.CSI.ApServer from CodeQL analysis
# This file is referenced by the codeql-action init step using `config-file`.
# Only analyze specified paths (Models, ApClient, Batch)
name: "Custom CodeQL Config"

# 只分析指定的路徑
paths:
  - NCB.CSI.Models
  - NCB.CSI.ApClient
  - NCB.CSI.Batch